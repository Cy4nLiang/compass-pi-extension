# 罗盘 Compass 运营使用手册

> 适用人群：Amazon US 精铺运营、选品主管。  
> 不要求会编程。日常使用以“直接对 pi 说中文”为主，只有少量 `/` 命令需要记住。

---

## 先看这一页：运营只需要会 3 件事

### 1. 打开罗盘

在项目目录启动 pi：

```bash
pi
```

进入 pi 后输入：

```text
/compass
```

看到“罗盘 COMPASS”总览，就表示加载成功。需要随时查看本手册时输入：

```text
/compass-help
```

### 2. 把 CSV 放进固定目录

将卖家精灵（西柚）、Sorftime 导出的 CSV 文件复制到：

```text
compass-imports/
```

这里指**宿主项目（也就是你运行 pi 的那个项目）根目录**下的 `compass-imports/`，不是 compass 扩展目录。首次使用请先创建：

```bash
mkdir compass-imports
```

例如：

```text
compass-imports/yoga-mat-strap.csv
```

> 文件必须在项目目录内；不要直接使用“下载”文件夹里的路径。建议文件名只用英文、数字和短横线。
>
> ⚠️ `compass-imports/` 与 `.pi/compass/` 都包含内部经营数据。如果宿主项目使用 Git，请把这两个目录一并加入宿主项目的 `.gitignore`，避免误提交。

### 3. 直接用中文告诉 pi 要做什么

例如：

```text
导入 compass-imports/yoga-mat-strap.csv，市场名叫 yoga mat strap，
数据来自卖家精灵，按“精铺·日均10单”模板做粗筛，并解释没通过的规则。
```

不需要记住 `compass_*` 工具名称。pi 会自动查找并启用对应工具。

---

# 一、罗盘能帮运营做什么

罗盘包含七个业务阶段，并额外保留“归档”结束状态：

```text
线索 → 粗筛 → 深研 → 风控 → 决策 → 测品 → 复盘
  └──────── 任一阶段明确停止后可进入归档 ────────┘
```

当前 Extension 可以完成：

1. 保存词根、竞品和榜单灵感；
2. 导入卖家精灵、Sorftime、Keepa 或通用 CSV；
3. 自动计算 QRD、CR3、HHI、AMZ 自营占比、新品占比等指标；
4. 按默认选品策略做粗筛；
5. 测算毛利、BE-CPC、CPC 承受度、净利和启动资金；
6. 保存差评痛点、改良方向和风险证据；
7. 管理七阶段主流程及归档状态；
8. 生成带数据来源、时间、决策记录和历史复盘的五维报告；
9. 统一查询时间线、相似市场、后验结果与经验卡；
10. 录入经营实绩、执行错杀回看和本地策略回测；
11. 记录各数据源成本和预算。

当前不会自动完成：

- 不会自动登录 Amazon、卖家精灵或 Sorftime；
- 不会自动取得 SP-API、Keepa 等付费数据；
- 不会替代律师、认证机构做最终判断；
- 不支持直接导入 `.xlsx`，请先导出为 CSV。

---

# 二、第一次使用

## 2.1 启动

在终端进入本项目目录，再启动 pi：

```bash
cd 项目目录
pi
```

如果出现“是否信任项目”提示，请确认当前确实是公司的罗盘项目，再选择信任。

如果 pi 已经开着，但刚更新过 Extension，输入：

```text
/reload
```

然后输入：

```text
/compass
```

## 2.2 判断是否正常

正常时可以看到六个页面：

- **总览**：市场数、候选数、Gate 结果、数据成本、待办计数、待复盘数和后验验证率；
- **待办**：需运营亲自处理的事项（P1–P5 优先级，自动推导；多数解决即消失，四类需提交处理结果并经验证后勾选）；
- **市场**：市场名称、Gate、Score、快照年龄；
- **候选池**：每个阶段有哪些候选；
- **预算**：各数据源本月使用情况与调用次数；
- **复盘**：到期队列、OutcomeCheck 分布和最近对照。

在总览里：

- `Tab` 或左右方向键：切换页面；
- `Esc`：关闭总览；
- 输入 `/compass`：随时重新打开。

---

# 三、10 分钟体验演示

先使用系统自带演示数据。它会在本地新增一条带 `demo` 名称的市场并计入看板；正式环境可跳过演示，或体验后让主管将它归档。

## 第 1 步：导入演示 CSV

输入：

```text
/compass-import .pi/extensions/compass/examples/demo-market.csv
```

根据提示填写：

| 提示 | 填写内容 |
|---|---|
| 市场/关键词族名称 | `yoga mat strap demo` |
| 数据来源 | `sellersprite` |

导入后会自动：

- 保存原始 CSV；
- 创建市场和候选卡；
- 计算市场指标；
- 跑一次粗筛 Gate。

## 第 2 步：看粗筛结果

直接说：

```text
解释 yoga mat strap demo 的粗筛结果，逐条告诉我：
QRD、新品占比、CR3、AMZ 自营占比，以及哪些规则通过或没通过。
```

## 第 3 步：做一遍利润测算

直接说：

```text
给 yoga mat strap demo 做利润测算：
售价 25.99 美元，采购 3.50，头程 0.90，关税 0，
佣金率 15%，FBA 5.20，CVR 12%，主词 CPC 0.85，
退货率 5%，目标日销 10，备货 60 天，
广告测试预算 500，一次性费用 100，总选品资金 20000。
保存到这个市场，并解释毛利和 CPC Gate。
```

## 第 4 步：生成报告

输入：

```text
/compass-report yoga mat strap demo
```

报告会显示在会话中，同时保存到：

```text
.pi/compass/reports/
```

自定义报告路径也必须位于该目录内并使用 `.md` 扩展名；`screen` 模式只执行策略中名为 `market_screen` 的阶段，阶段缺失或规则为空会转人工复核。

演示市场没有完整的真实风险证据，因此完整报告出现“待复核”是正常的，不要为了变绿而编造证据。

---

# 四、真实选品标准流程

## 阶段 1：记录线索

只有词根、竞品或榜单灵感，还没有 CSV 时，直接说：

```text
把“折叠露营灯”建成一个罗盘市场线索。
关键词：foldable camping light、portable tent light。
类目：Sports & Outdoors。
负责人：小李。
标签：户外、轻小件。
```

系统会创建：

- 一个市场档案；
- 一张处于“线索”阶段的候选卡；
- 一条创建记录。

此时没有数据快照，不能做 Gate 判断。下一步应导出 CSV。

---

## 阶段 2：准备并导入 CSV

### 4.2.1 从现有工具导出

在卖家精灵或 Sorftime 中完成市场查询后，使用产品自身的“导出 CSV”功能。

不要：

- 不要把账号密码发给 pi；
- 不要把截图当作完整 CSV；
- 不要把 `.xlsx` 直接改后缀为 `.csv`；
- 不要删除不理解的列，原始列越完整越好。

### 4.2.2 放入导入目录

把文件复制到：

```text
compass-imports/
```

建议命名格式：

```text
日期-市场-来源.csv
```

例如：

```text
2026-08-22-yoga-mat-strap-sellersprite.csv
```

### 4.2.3 导入

最简单的方式是输入：

```text
/compass-import compass-imports/2026-08-22-yoga-mat-strap-sellersprite.csv
```

然后选择正确来源：

| 选项 | 什么时候选 |
|---|---|
| `sellersprite` | 卖家精灵/西柚导出 |
| `sorftime` | Sorftime 导出 |
| `keepa` | Keepa 导出 |
| `manual_csv` | 公司人工整理的标准 CSV |
| `generic_csv` | 其他工具或不确定模板 |
| `auto` | 确实无法判断时才选 |

也可以直接说：

```text
导入 compass-imports/2026-08-22-yoga-mat-strap-sellersprite.csv，
市场名叫 yoga mat strap，来源是卖家精灵，导入后自动粗筛。
```

### 4.2.4 最低字段要求

要完整完成粗筛，CSV 最好包含：

| 字段 | 用途 | 缺失后影响 |
|---|---|---|
| ASIN 或标题 | 识别商品 | 两者都没有则无法导入 listing |
| 排名 | 确定 Top100 和腰部区间 | 腰部口径可能不准确 |
| 月销量 | QRD、市场容量、集中度 | 多个核心 Gate 无法计算 |
| 品牌 | CR3、CR5、HHI | 无法判断品牌垄断 |
| 卖家类型/是否 Amazon 自营 | AMZ 自营占比 | 无法判断自营压力 |
| 上架日期或上架月数 | 新品占比 | 无法判断市场活跃度 |
| 价格 | 价格带、销售额 | D1/D3 信息不完整 |
| 星级、评论数 | 评论护城河、低分高销 | D2/D4 信息不完整 |
| 关键词、搜索量、CPC | 流量结构、CPC | 深研时仍需补数 |

字段缺失时，系统会显示“缺数据”或“待复核”，不会假装通过。

---

## 阶段 3：看懂粗筛结果

导入后，先说：

```text
解释这个市场的粗筛结果。只使用已导入快照，
列出每条 Gate 的值、阈值、结果、来源和采集时间。
```

### 默认粗筛 Gate

| 指标 | 默认判断 | 运营理解 |
|---|---|---|
| AMZ 自营占比 + CR3 | AMZ>30% 且 CR3>60% → 否决 | 自营和头部品牌同时强势，进入困难 |
| 新品占比 | ≥15% | 市场仍在接纳新链接 |
| QRD(300) | ≥20 | 至少 20 个坑位能达到月销 300，即日均约 10 单 |

### 三种结论

| 结论 | 含义 | 下一步 |
|---|---|---|
| `pass / 通过` | 当前阶段硬门槛通过 | 进入深研 |
| `review / 待复核` | 缺数据或在边界区间 | 补数据或人工判断 |
| `reject / 否决` | 触发 veto 或未满足硬门槛 | 写明原因后归档，通常不继续花钱 |

> “待复核”不是“差”，而是系统拒绝在证据不足时替你下结论。

### 批量扫描

一周导入多个市场后，可以说：

```text
扫描全部市场，只看粗筛通过的候选；
要求 QRD 至少 20、新品占比至少 15%，按综合分从高到低排序。
```

批量扫描的 Score 按同批候选的分位数计算，适合排序；不要把不同批次的 Score 当作绝对值直接比较。

---

## 阶段 4：利润与流量成本深研

粗筛通过后，准备以下数据：

| 数据 | 示例 | 来源建议 |
|---|---:|---|
| 售价 | $25.99 | 目标售价/市场价格带 |
| 采购价 | $3.50 | 工厂报价，先换算成美元 |
| 头程分摊 | $0.90/件 | 货代报价 |
| 关税分摊 | $0.00/件 | 实际税则测算 |
| 佣金率 | 15% | Amazon 最新费率 |
| FBA 配送费 | $5.20 | 最新官方费用测算 |
| CVR | 12% | 类目基准或自有数据 |
| 主词 CPC | $0.85 | 卖家精灵/广告数据 |
| 退货率 | 5% | 类目或自有数据 |
| 目标日销 | 10 | 精铺目标 |
| 备货天数 | 60 | 资金计划 |
| 广告测试预算 | $500 | 测品计划 |
| 一次性费用 | $100 | 认证、摄影、模具等 |
| 总选品资金 | $20,000 | 用于检查单 SKU 是否超过 20% |

### 重要口径

1. 所有金额必须使用同一种币种；建议统一美元。
2. 百分比不要混淆：15% 等于 0.15。
3. 采购价如果是人民币，请先按公司汇率换算成美元。
4. CVR、退货率如果只是估计，要明确告诉 pi“这是暂定假设”。
5. FBA 和佣金要使用最新费率，不要抄旧报告。

### 推荐说法

```text
给 [市场名] 做利润测算并保存：
售价 __，采购 __，头程 __，关税 __，佣金率 __%，FBA __，
CVR __%，CPC __，退货率 __%，目标日销 __，备货 __ 天，
广告预算 __，一次性费用 __，总选品资金 __。
请输出毛利率、BE-CPC、CPC承受度、三档TACOS净利和回本周期。
```

### 默认经济 Gate

| 指标 | 默认判断 |
|---|---|
| 毛利率 | ≥40% |
| CPC 承受度 | ≤0.60 通过；0.60–0.80 待复核；>0.80 否决 |
| 单 SKU 资金占比 | >总选品资金 20% 时待复核 |

---

## 阶段 5：差评与产品力

建议分析 Top10 竞品的 1–3 星评论。

可将官方导出的评论或整理后的评论文件放进项目，再说：

```text
分析 [市场名] Top10 竞品的 1–3 星评论。
按质量、尺寸、运输损坏、期望落差、使用困惑、其他进行聚类，
每个主题给出数量、证据原句、可改良性和改良建议。
分析完成后写入罗盘。
```

可改良性含义：

| 值 | 含义 |
|---|---|
| `factory` | 工厂、材料或结构可以解决 |
| `packaging` | 包装可以解决 |
| `copy` | 图片、文案、说明书可以解决 |
| `none` | 当前资源无法解决 |
| `unknown` | 还没判断清楚 |

不要只写“用户觉得不好”。要保留具体痛点和评论原句，例如：

```text
“背带调到最长仍然太短” → 尺寸问题 → 工厂可改 → 增加 20cm 调节范围
```

---

## 阶段 6：风险核查

### 风险五类

1. 认证：CPC/CPSIA、FCC、FDA、UL/ETL、UN38.3 等；
2. 知识产权：外观专利、实用专利、商标、版权；
3. 季节性：销量波动、Google Trends 峰谷；
4. 政策/擦边：医疗功效、驱蚊、成人、武器配件、受限品类等；
5. 物流：带电、液体、粉末、磁性、易碎、超尺寸。

### 推荐说法

```text
核查 [市场名] 的认证、专利商标版权、季节性、Amazon政策和物流风险。
只使用最新官方来源；每项都给网址和核查日期。
查不清的填 unknown，需要专业判断的填 review，不要凭模型记忆判绿。
完成后写入罗盘。
```

如果当前 pi 没有网页检索能力，运营应人工查询后，把官方链接发给 pi：

```text
这是我核验过的官方链接：
认证：...
政策：...
专利：...
请根据这些证据更新 [市场名] 风险记录；没有证据的项目保持 unknown。
```

### 状态含义

| 状态 | 含义 |
|---|---|
| `pass` | 有足够证据支持通过 |
| `review` | 需要人工、主管、律师或认证机构判断 |
| `red` | 已发现明确红色风险 |
| `unknown` | 尚未查清 |

即使所有选项手工填写为 `pass`，如果没有可点击证据链接，系统也会把总体风险降级为 `review`。

---

## 阶段 7：运行完整策略并出报告

深研和风控完成后，说：

```text
对 [市场名] 运行“精铺·日均10单”完整策略。
逐条解释所有 missing、review、fail 和 veto，不要只给总分。
```

确认结果后生成报告：

```text
/compass-report [市场名或market_id]
```

报告包括：

- 总结论和综合分；
- 全部 Gate；
- 五维指标与证据；
- 利润三情景；
- 差评痛点；
- 风险记录；
- 多源偏差；
- 决策历史；
- 下一步建议；
- 第 9 节“历史与复盘”：OutcomeCheck 与命中的 active lessons。

---

## 阶段 8：移动候选卡

不要只在聊天里说“进入深研”，要让系统记录阶段变化。

例如：

```text
把 [市场名] 从粗筛移动到深研。
原因：QRD=28、新品占比=21%、未触发红海否决，主管同意继续。
```

归档示例：

```text
把 [市场名] 归档。
原因：AMZ自营占比35%且CR3为68%，触发红海一票否决，不继续购买深研数据。
```

每次移动都必须填写原因，这是复盘时最重要的记录之一。

---

## 阶段 9：结果复盘与经验沉淀

输入：

```text
/compass-retro
```

系统会按“到期列表 → 逐项对照/实绩 → 复盘报告 → 是否保存经验卡”推进。也可以直接告诉 pi：

```text
列出全部到期复盘。给 [市场名] 回放当时决策和后来证据。
```

### 三种后验结论

| verdict | 含义 | 操作纪律 |
|---|---|---|
| `validated` | 当时判断得到后来证据支持 | 仍保留原始证据，不代表收益承诺 |
| `challenged` | 当时前提受到挑战，可能错杀或漏放 | 只提示人工复看，不自动翻转候选状态 |
| `inconclusive` | 新快照、实绩或关键字段不足 | 补证据，不得写成验证成功 |

- go/testing 品录入实际日销、TACOS、退货率和净利率；日销达到策略目标 70% 且净利为正才记为 validated，日销低于 40% 或净利为负记为 challenged。
- no_go/waitlist 在新快照到场后重放当时规则。新快照与基线相隔至少 7 天时，CSV 导入会自动做一次对照。
- Lesson 必须附现存的 `chk_*`、`dec_*` 或 `run_*` evidence；过时经验应填写原因后退役，不能删除。
- 正式调阈值前，先让 pi 用 `compass_retro backtest` 比较新旧策略（支持 `策略id@v版本`），再保存新版本。

默认到期周期：go 每 30 天、testing 停留 60 天、waitlist 45 天、no_go 90 天抽样、review 30 天。主管可在策略 `meta` 的 `retro_*_days` 字段调整。

需要全量历史时使用 `compass_history`，可查 timeline、search、similar、outcomes、lessons。罗盘会在相关选品 prompt 前自动注入有上限的历史速览，并给关键工具结果加历史尾注；本会话可用 `/compass-history-brief off` 关闭、`on` 恢复。

---

# 五、如何看 `/compass` 看板

## 总览

重点看：

- 市场和候选数量；
- 30 天以上过期快照数量；
- 通过、复核、否决数量；
- 待办数量（P1/P2 有数字就先去待办页）；
- 当前预算是否熔断。

## 待办

待办页列出所有**需要运营亲自处理**的事项，由系统从数据状态自动推导。逾期超过 30 天的事项自动升 1 级。

大多数事项**解决即消失**，不用手动勾选（导入了新 CSV、填了决策原因、补了风险证据，条目自己就没了）。但有四类事项系统看不见你在系统外做了什么，必须走**人工闭环**——详见下面「## 需要提交处理结果的四类待办」。

| 优先级 | 含义 | 典型事项 |
|---|---|---|
| P1 紧急阻塞 | 当天处理 | 预算熔断；复盘结论 challenged（前提受挑战）待处置 |
| P2 漏斗阻塞 | 阻塞候选推进 | Gate=review 待人工复核；决策评审待 go/waitlist/no_go |
| P3 补数据/补证据 | 本周安排 | 深研缺 CPC/毛利等硬指标；风险清单未做或缺官方证据链接 |
| P4 例行到期 | 按节奏处理 | 到期复盘（含测品录实绩）；预算 80% 告警 |
| P5 保鲜/优化 | 有空处理 | 快照超 30 天；多源指标偏差 >30% 待定口径 |

每条待办都带「建议动作」，照着执行即可。页内每组最多显示 3 条，全部清单在会话里说：

```text
列出当前全部待办，先给 P1 和 P2。
```

（对应工具为 `compass_todo`，可按 priority/kind/市场过滤。）

## 需要提交处理结果的四类待办

这四类的**人工处理动作系统看不见**——你在系统外定了口径、做了决定，账面上什么都没变，条目就一直挂着：

| 待办 | 系统看不见的那部分 | 什么情况下它仍会自动消失 |
|---|---|---|
| 多源指标偏差 >30% | 「以哪个来源为准」是你的判断，不落在任何字段里 | 新导出让各来源重新对齐（偏差回到 30% 以内），或候选归档 |
| 预算 80% 告警 | 你核对完用量决定「收紧补数」，账面数字不会变 | 提额、用量回落、次月重置 |
| 预算熔断 | 你决定「本月接受停摆」，配额仍然是满的 | 提额解除、次月自动恢复 |
| 深研缺硬指标 / 深研数据待人工确认 | 指标齐了不等于真去供应商问过成本 | **只有候选移出深研阶段才消失——指标补齐后条目不会自动消失**，会转成「深研数据待人工确认」等你确认 |

换句话说：条件本身被系统内动作解决了（提了额、来源对齐了），条目照样自己消失，不用提交任何东西；**闭环通道针对的是系统感知不到的那类处理**——「本月接受停摆」「以 sellersprite 为准」这种只存在于你脑子里的决定。深研类是唯一的例外：指标齐备只是判据之一，必须走完闭环才关闭。

闭环走**提交 → 验证 → 勾选**三步，防的是「自称处理了但没处理到位，事后被重复排查」：

1. **提交处理结果**（你做）：在 Web 待办页点该行的「提交处理结果」，填**处理说明**（做了什么、结论是什么、关键数值）和**证据**（链接或项目内文件路径，0 到多条）。说明不能为空。
2. **验证**（agent 做，在 pi 会话里）：回到 pi 会话说一句「验证待办」，agent 会逐条核对你的说明与系统里的实况，给出通过或驳回。**浏览器里没有验证按钮**——验证需要 AI 判断，Web 页面做不了。
3. **勾选已处理**（你做）：验证通过后该行出现「勾选已处理」，点两下（第二下是确认）即关闭；条目离开活跃清单，进入「已处理」筛选项。

被驳回时行内直接显示驳回理由，按理由补充材料后重新提交即可，历史提交全部留档。勾错了或后来发现没处理好，在「已处理」里点「重开」（必须写理由），条目回到活跃清单。

**提交里要写什么才会通过**（agent 照这张表验，写不到位会被驳回）：

| 待办 | 提交说明里必须写清 | 系统会先硬查（不满足时 agent 想通过也会被拒绝） |
|---|---|---|
| 多源指标偏差 >30% | 以**哪个来源为准**，以及为什么 | — |
| 预算 80% 告警 | 用量核对结论 + 后续动作（提额 / 收紧补数 / 接受现状） | — |
| 预算熔断 | 明确决定（提额 or 本月接受停摆）+ 理由 | — |
| 深研数据 | 找了哪家**供应商**、问的是**哪个 SKU**、**成本构成**是什么 | ① 四项深研硬指标齐备；② 这个市场在罗盘里**存过利润测算**（`compass_profit_estimate` 保存过） |

深研类最容易白跑一轮：只写「已向供应商问过成本」但没在罗盘里保存利润测算，提交能过、验证一定被驳回。先做测算再提交。

行上的状态徽标（在标题右边、操作按钮左边）就是当前进度：

| 徽标 | 含义 | 下一步 |
|---|---|---|
| 未处理 | 还没提交过 | 处理完点「提交处理结果」 |
| 待验证 | 已提交，球在 agent 那边 | 回 pi 会话说「验证待办」 |
| 已驳回 | 验证没过，理由就在同一行 | 按理由补材料，重新提交 |
| 验证通过·待勾选 | 可以关闭了 | 点「勾选已处理」并确认 |
| 已重开·待重新提交 | 拉回来了 | 重新提交处理结果 |
| 已处理·失效浮出 | 关闭后又出现新情况 | 见下 |

**为什么已经勾掉的又冒出来了**：勾选时系统记下了一个「水位」，出现新情况就会重新提醒你——预算类到了新的月份、偏差类的比较来源被新导出替换、深研类的候选重新进入深研或指标回退。这是刻意设计的：宁可多提醒一次，也不让问题被永久静默。处理方式是点「重开」后重新提交。

「已处理」筛选项里能看到每条的提交说明、证据、验证结论与理由，以及提交/验证/勾选三个动作分别是谁在什么时候做的——事后追责或复盘时按这个回看。

## 市场

| 显示 | 含义 |
|---|---|
| Gate | 最近一次策略结果 |
| Score | 排序分，不代替 Gate |
| `3d`、`20d` | 快照距今天数 |
| 来源 | sellersprite、sorftime 等 |

粗筛快照超过 30 天应重新导入；深研数据通常应在 7 天内。

## 候选池

对应阶段：

| 系统值 | 中文 |
|---|---|
| `lead` | 线索 |
| `screen` | 粗筛 |
| `deep_research` | 深研 |
| `risk` | 风控 |
| `decision` | 决策 |
| `testing` | 测品 |
| `review` | 复盘 |
| `archived` | 归档 |

阶段移动必须填写并保存 reason。Gate 结果之外，最终经营决策使用：

| 状态 | 含义 | 操作 |
|---|---|---|
| `go` | 通过评审，进入下一步投入 | 必须填写投入依据、负责人和时间 |
| `waitlist` | 证据不足或暂缓 | 必须填写待补证据和复核条件 |
| `no_go` | 明确不投入 | 必须填写否决依据；候选仍保留，不删除 |

示例：

```text
用 compass_pool 把 [市场名] 记录为 waitlist。
原因：毛利已达标，但风险官方证据和评论样本不足，补齐后再复核。
```

所有阶段、Gate 和最终决策状态都必须有原因，并会写入 `decision_log`。

对单个候选做决策时可以直接核对 Amazon 实况：浏览器工作台的单品决策页（见「浏览器版工作台」）和 `compass_pool` 的 get 输出都会带市场关键词的 Amazon 搜索链接（至多 3 条）与 Top5 竞品 listing 链接（排名/价格/星级/月销参考）。链接在你自己的浏览器会话里打开；行内数字来自本地快照，不是实时数据。尚未导入 CSV 的线索没有竞品链接，会显示占位提示。

## 预算

| 状态 | 含义 |
|---|---|
| 正常 | 可继续按计划使用 |
| 80% 告警 | 接近月度上限，先确认剩余额度 |
| 熔断 | 已到上限，不应强行调用付费源 |
| 免费 | 配置为零增量成本来源 |
| 禁用 | 当前不允许使用 |

运营不要使用 `force=true` 绕过预算熔断，应先找主管确认。

### Sorftime MCP 调用计量

pi 会话里每次调用 Sorftime MCP（在线拉类目、垄断率等）都会被罗盘**自动计数**，显示在预算页的「调用」列（所有来源统一显示当月次数，0 次起）。默认只计数、不折算成本、不设上限；要开启成本折算或次数配额，让 pi 执行：

```text
配置 sorftime 预算：每次调用 0.5 元，每月上限 200 次。
```

（对应 `compass_budget action=configure source=sorftime cost_per_call_cny=0.5 monthly_call_limit=200`；`monthly_call_limit=0` 表示清除次数上限。）

配置上限后：金额或次数**任一**达到 80% 触发告警、100% 熔断；熔断时罗盘会直接**拦截**后续 Sorftime 调用，并在提示里给出解除方式——提高上限（同一条 configure 指令）或等次月自动恢复。

三条已知局限：

- `mcpScript` 批量脚本内部的调用无法归因，不计入次数；
- pi 会话异常退出（崩溃/强杀）会丢失最后一批尚未落盘的计数；正常退出与日常操作会自动落盘；
- 计数按落盘时刻归属月份：跨月交界时最后一批未落盘的调用会计入落盘当月，量级可忽略。


## 复盘

重点看：

- 哪些 go、waitlist、no_go 或 review 候选已经逾期；
- validated / challenged / inconclusive 的数量；
- 最近 OutcomeCheck 和 active Lesson 数量。

逐项处理请运行 `/compass-retro`；完整台账与策略版本准确率用 `compass_history action=outcomes`。

## Web 工作台（浏览器版）

不习惯 TUI 键盘操作，也可以在浏览器里看同样的数据，多两页（市场档案、导入向导）：

```text
/compass-web
```

会提示一个本机地址（默认 `http://127.0.0.1:4780`），浏览器直接打开即可；候选池点击候选卡会进入全屏单品决策页——Amazon 搜索与 Top5 竞品链接、关键指标、五维分、利润、风险、Gate 规则和决策日志一屏看全，填写理由后直接移动阶段或记录决策，效果与 `compass_pool` 一致，都会写入 `decision_log`，成功后自动返回看板。

待办页还是四类闭环事项的操作台：提交处理结果、勾选已处理、重开都在这里点；验证按钮不在这（要 AI 判断，得回 pi 会话）。

市场档案页右上角点「生成五维报告」，报告会直接以排版好的弹窗打开——左边是九章目录，点一下跳到对应小节；表格、结论、证据链接都是渲染好的样子，不再是一堆 Markdown 符号。右上角可以切「Markdown 源码」原样查看、或一键「复制 Markdown」，按 Esc 或点弹窗外面关闭。关掉之后面板上会留一个「查看最近报告」，重看不会重新生成（只在这次停留于市场档案页期间有效——离开这一页再回来按钮就没了，重新生成一次、或直接去 `.pi/compass/reports/` 打开那份 `.md` 即可）。报告文件本身还是照旧存成 `.pi/compass/reports/<市场ID>-<日期>.md`，弹窗只是换了个好读的看法。

- 指定端口：`/compass-web 5000`；
- 关闭：`/compass-web stop`（正常结束会话也会自动关闭，不用每次手动关）；
- 脱离 pi 单独启动（比如给同事临时看一眼数据）：在项目的 `.pi/extensions/compass/` 目录下执行 `npm run web`。

**只有本机能访问，不要用花生壳、ngrok 等工具转发到外网或公司内网**——这台工作台能读到全部选品经营数据，跟直接打开 `store.json` 没有区别。

---

# 六、常见指标解释

| 指标 | 简单理解 |
|---|---|
| QRD(300) | Top100 中月销≥300 的链接数；越深，达到日均10单的坑位越多 |
| CR3 | Top3 品牌的销量份额；越高越垄断 |
| HHI | 品牌集中程度；>2500 通常代表高集中 |
| AMZ 自营占比 | Amazon 自营销量份额；高占比代表价格和 Buy Box 压力 |
| 新品占比 | 上架≤12个月的链接占比；高说明市场仍接纳新品 |
| 腰部月销 | 排名 P40–P60 链接的月销中位数 |
| 低分高销数 | 星级≤4.2 且月销≥300 的链接数；可能存在改良机会 |
| 毛利率 | 不含广告和退货的基础毛利率 |
| BE-CPC | 广告点击单价最多能承受到多少才不亏毛利 |
| CPC 承受度 | 实际/建议 CPC ÷ BE-CPC；越低越安全 |
| 预估星级差 | 新品预估星级－腰部竞品星级 |
| 置信度 | 数据证据是否完整、来源是否可靠；低置信度需要复核 |

### Score 和 Gate 的区别

- **Gate 决定能不能继续**；
- **Score 用来给已进入比较范围的候选排序**；
- 高分但触发 veto，仍然应该否决；
- 低分但 Gate 通过，可以进入人工比较，不代表一定不能做。

---

# 七、常用中文指令模板

复制后把方括号内容替换掉即可。

## 建线索

```text
把 [产品/市场名] 建成罗盘线索。
关键词：[关键词1、关键词2]；类目：[类目]；负责人：[姓名]；标签：[标签]。
```

## 导入并粗筛

```text
导入 [CSV路径]，市场名叫 [市场名]，来源是 [卖家精灵/Sorftime]。
导入后运行默认粗筛，并解释所有未通过或缺失的规则。
```

## 批量找机会

```text
扫描全部市场，只保留粗筛通过、QRD≥20、新品占比≥15%的市场，
按同批综合分排序，并列出数据快照年龄。
```

## 利润测算

```text
给 [市场名] 做利润测算并保存：售价__，采购__，头程__，关税__，
佣金__%，FBA__，CVR__%，CPC__，退货率__%，日销目标__，
备货__天，广告预算__，一次性费用__，总资金__。
```

## 查缺失数据

```text
告诉我 [市场名] 跑完整策略还缺哪些字段。
按粗筛、利润、产品力、风险分组，并给出最便宜的补数方式。
```

## 差评分析

```text
分析 [市场名] 的低星评论，输出主题、数量、证据原句、可改良性和建议，
完成后写入罗盘。
```

## 风险核查

```text
核查 [市场名] 五类风险，只引用最新官方来源并保留URL。
无法确认的写 unknown，需要专业判断的写 review，禁止凭记忆判绿。
```

## 完整策略

```text
对 [市场名] 运行默认完整策略，逐条解释所有非通过项，并告诉我下一步补什么。
```

## 移动候选

```text
把 [市场名] 移动到 [深研/风控/决策/测品/复盘/归档]。
原因：[写清指标、证据和审批结论]。
```

## 历史与复盘

```text
查询 [市场名] 的完整 timeline、前三个相似历史市场、命中的经验卡，
并列出所有到期复盘对象。不要直接读取 store.json。
```

```text
对新策略 [策略id@vN] 和基线 [策略id@vM] 做 backtest，
输出 outcome 翻转矩阵和与 OutcomeCheck 的一致率，不持久化运行结果。
```

## 周报

```text
根据罗盘候选池生成本周选品周报：
新增线索、粗筛通过、否决原因、深研进度、复盘验证率、风险待办、预算消耗和下周动作。
```

---

# 八、常见问题与处理方法

## 8.1 输入 `/compass` 提示命令不存在

按顺序处理：

1. 输入 `/reload`；
2. 确认是在本项目目录启动的 pi；
3. 确认信任了当前项目；
4. 仍不行时把完整报错发给开发，不要自行改 Extension 文件。

## 8.2 CSV 路径不允许或不在项目内

把 CSV 复制到：

```text
compass-imports/
```

然后使用相对路径：

```text
/compass-import compass-imports/文件名.csv
```

## 8.3 提示找不到文件（ENOENT）

检查：

- 文件名是否拼错；
- `.csv` 后缀是否真实存在；
- 是否把空格、中文括号写错；
- 是否放到了另一个项目的目录。

最简单的处理方式是把文件改成短英文名，例如：

```text
market-01.csv
```

## 8.4 `.xlsx` 无法导入

回到原工具，选择“CSV 导出”；或用 Excel/WPS 另存为 UTF-8 CSV。

不要只修改文件后缀。

## 8.5 导入成功，但很多指标显示缺失

通常是 CSV 没有对应列。直接说：

```text
检查刚导入的市场，列出成功映射的字段和缺失字段，
告诉我应该从卖家精灵或 Sorftime 补导哪些列。
```

## 8.6 为什么所有完整策略结果都是待复核

常见原因：

- 只导入了市场 CSV，还没做利润测算；
- 没有主词 CPC；
- 没有差评分析和预估星级；
- 风险项没有官方证据链接；
- 单 SKU 总资金没有填写。

说：

```text
列出 [市场名] 完整策略的 missing 指标，按优先级给我补数清单。
```

## 8.7 风险项明明填了 pass，为什么总体仍是 review

因为没有可点击证据链接。把官方来源 URL 补上；没有证据时应保持 review/unknown。

## 8.8 找不到市场

输入 `/compass` 查看市场列表，或说：

```text
列出所有市场的 market_id 和名称。
```

后续优先使用 `market_id`，避免同名。

## 8.9 重复导入同一个市场会怎样

同一市场的**不同日期新 CSV** 会追加不可变快照并触发历史对照，不会覆盖旧快照；完全相同文件的 SHA-256 会被拦截，避免重复快照污染统计。

建议始终使用同一个标准市场名，例如一直使用：

```text
yoga mat strap
```

不要今天叫 `yoga strap`，明天又叫 `yoga mat carrier`，否则会产生两个市场。

## 8.10 预算显示熔断

停止调用该付费数据源，先联系主管。不要让 pi 强制记账或绕过上限。

Sorftime MCP 等计量源熔断后，罗盘会直接拦截后续调用并给出提示。经主管确认需要继续时，用 `compass_budget configure source=sorftime monthly_call_limit=更大数值`（或提高 `monthly_limit_cny`）解除；不处理则次月自动恢复。

## 8.11 报告里的数据与网页今天看到的不一致

先检查报告引用的快照日期。报告复现的是“当时入库的数据”，不是自动读取今天网页上的值。

需要更新时重新导出 CSV，并以同一个市场名称导入新快照。

---

# 九、数据安全与操作红线

1. 不把 Amazon、卖家精灵、Sorftime 的账号密码发给 pi；
2. 不把 API key 写进聊天、CSV 或策略 YAML；
3. 不做刷单、测评、跟卖等违规操作；
4. 不对外转售采集数据；
5. 采集环境与卖家主账号环境必须物理隔离；
6. 不凭模型记忆把认证、专利或政策风险判绿；
7. 不直接读取或手工修改 `.pi/compass/store.json` / `snapshots/*.json`；用 `compass_history`、`compass_asin_history`、`compass_keyword_metrics` 渐进查询；
8. 不删除被否决候选，应归档并保留原因；
9. 不用高 Score 推翻红色 Gate；
10. 不绕过预算熔断。

业务数据保存在：

```text
.pi/compass/
```

其中：

- `store.json`：市场、策略、候选、风险、决策日志、OutcomeCheck、Lesson 及快照元数据；
- `raw/`：导入 CSV 的原始归档；
- `snapshots/`：按快照保存的 listing/关键词明细；
- `reports/`：生成的选品报告与 `retro-YYYY-MM-DD.md` 复盘报告。

需要备份时，整体复制 `.pi/compass/`。不要只复制报告而丢掉快照和决策日志。

---

# 十、运营每日/每周检查表

## 每日

- [ ] 打开 `/compass` 待办页，P1/P2 事项当天处理完；
- [ ] 闭环四类（偏差口径 / 预算告警 / 熔断 / 深研数据）处理完当天就在 Web 待办页提交处理结果；
- [ ] 看到「待验证 n」就回 pi 会话说一句「验证待办」，别让它过夜；
- [ ] 验证通过的条目当天勾选掉，「已驳回」的按理由补材料重提；
- [ ] 新线索是否已录入，而不是只留在聊天记录；
- [ ] 新 CSV 是否使用统一市场名导入；
- [ ] 粗筛非通过项是否逐条看过；
- [ ] 候选阶段变化是否填写原因；
- [ ] 风险结论是否附证据链接；
- [ ] 付费数据成本是否关联到具体市场。

## 每周

- [ ] `/compass` 中是否有超过 30 天的粗筛快照；
- [ ] 本周新增市场是否达到团队目标；
- [ ] 粗筛通过市场是否完成利润测算；
- [ ] `review` 候选是否有明确补数负责人；
- [ ] 否决候选是否归档且保留原因；
- [ ] 预算是否达到 80% 告警或熔断；
- [ ] `/compass-retro` 是否还有到期对象；在测产品是否按 30/60/90 天计划复盘；
- [ ] challenged 是否已人工复看，Lesson 是否都挂了 evidence。

---

# 十一、主管建议

初级运营建议只开放以下动作：

- 建线索；
- 导入 CSV；
- 查看粗筛；
- 填利润参数；
- 整理差评和官方风险证据；
- 生成报告；
- 按审批结果移动候选。

以下动作建议由主管或熟练运营完成：

- `/compass-strategy` 修改策略阈值；
- 新建或复制策略版本；
- 修改数据源预算；
- 对边界 Gate 人工放行；
- 对红色风险执行升级复核或否决；
- 清理或迁移 `.pi/compass/` 数据。

策略一旦修改会生成新版本，不覆盖旧版本。正式改阈值前，应写清修改原因，先用 `compass_retro action=backtest` 在不可变历史快照上比较新旧版本，再由主管审核后保存；回测只消费本地快照与已录入 OutcomeCheck，不会联网补造 6 个月数据。
