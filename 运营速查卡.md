# 罗盘运营速查卡

## 每次开始

```text
/compass
```

查看完整手册：

```text
/compass-help
```

如果命令不存在：

```text
/reload
```

## 想用浏览器看（可选）

```text
/compass-web
```

打印本机地址，浏览器打开即可；指定端口 `/compass-web 5000`，关闭 `/compass-web stop`。脱离 pi 单独起：**项目根目录**（平时启动 pi 的那个目录）下 `node --experimental-strip-types .pi/extensions/compass/web/standalone.ts`；跑错目录只会起一个空数据的工作台。**只能本机访问，不要转发到外网/内网；同事要看数据请各自本机导入同一份 CSV。**

## 每天先看待办

`/compass` 第 2 页「待办」：P1 当天处理、P2 别拖；多数事项解决后自动消失。会话里直接说：

```text
列出当前全部待办，先给 P1 和 P2。
```

（工具：`compass_todo`，可按优先级/类型/市场过滤。）

## 四类要提交处理结果的待办

**多源偏差口径 / 预算 80% 告警 / 预算熔断 / 深研数据**——你在系统外定的口径和决定，系统看不见，条目不会因此消失（条件本身被系统内动作解决时照样自动消失：提额、次月重置（UTC 月界 = 北京时间 1 日 08:00）、来源重新对齐；**只有深研类指标补齐了也不消失**）。

三步走（第 1、3 步在 Web 待办页 `/compass-web` 点，第 2 步回 pi 会话）：

1. **提交**处理说明 + 证据（链接或文件路径）——偏差写明以哪个来源为准及理由；预算写用量结论与后续动作 / 停摆或提额的决定；深研写供应商、SKU、成本构成，**并先存好利润测算**；
2. 回 pi 会话说一句「**验证待办**」——验证要 AI 判断，浏览器里没这个按钮；
3. 通过后点「**勾选已处理**」再确认；被驳回就按行内理由补材料重提。

看行上的状态徽标（标题右边）就知道到哪一步了：未处理 → 待验证 → 已驳回 / 验证通过·待勾选 →（勾选后进「已处理」）。勾错了在「已处理」里点「重开」（写理由）。

**已经勾掉的又冒出来（「已处理·失效浮出」）** = 出现了新情况：新预算月、比较来源被新导出替换、候选重回深研或指标回退。这是防漏提醒的设计，点「重开」后重新提交。

## Sorftime 调用与预算

预算页「调用」列显示各源当月 MCP 调用次数（自动计量）。**结算月是 UTC 月**，标题写作 `2026-08 (UTC)`。要设单价或次数上限：

```text
配置 sorftime 预算：每次调用 0.5 元，每月上限 200 次。
```

达上限即熔断并拦截后续调用；解除靠提高上限（`monthly_call_limit`，0=清除）或等 UTC 次月自动恢复——北京时间**每月 1 日 08:00 整**清零，1 日凌晨 0–8 点的调用仍记上个月。把源设为 `enabled=false` 禁用后，该源的 MCP 调用同样会被拦截，提示里给出 `enabled=true` 的恢复方式。

计次口径：成功、服务端报错、30 秒超时、发出后被中断都算一次；认证/连接/退避/审批失败（请求没发出去）不算。

## 导入 CSV

1. 把 CSV 放到项目的 `compass-imports/`；
2. 输入：

```text
/compass-import compass-imports/文件名.csv
```

3. 市场名称每次保持一致；
4. 选择正确来源：卖家精灵=`sellersprite`，Sorftime=`sorftime`；用户主动浏览器采集使用=`compass_browser`。

导入后**先看告警**：占位文字（暂无/未知/TBD/?/--）一律按缺数据算、不当成 0；AMZ 自营占比要求卖家类型与月销量同时有值、有效样本不足半数会转人工复核（告警会说清缺哪一列）；关键词被复制到每行时会去重并提示宽表。文件请存成 UTF-8 CSV，乱码表头会被拒绝导入。

## 粗筛解释

```text
解释 [市场名] 的粗筛结果，列出 QRD、新品占比、CR3、AMZ占比，
逐条说明 Gate、来源、日期和缺失数据。
```

## 利润测算

```text
给 [市场名] 做利润测算并保存：售价__，采购__，头程__，关税__，
佣金__%，FBA__，CVR__%，CPC__，退货率__%，日销__，备货__天，
广告预算__，一次性费用__，总选品资金__。
```

金额统一使用同一种币种，建议美元。

## 风险核查

```text
核查 [市场名] 五类风险，只用最新官方来源并保留URL。
查不清填 unknown，需要专业判断填 review，不准凭记忆判绿。
```

## 完整策略与报告

```text
对 [市场名] 运行默认完整策略，解释全部非通过项。
```

```text
/compass-report [市场名]
```

报告默认写入 `.pi/compass/reports/`；自定义路径也必须在该目录内并使用 `.md`。自定义 `screen` 策略必须有 `market_screen` 规则，否则转人工复核。市场报告第 9 节会自动带上 OutcomeCheck 与命中的经验卡。指定策略时写策略 id、策略名称或 `策略id@v版本`（锁定历史版本，不写版本后缀就是最新版）——扫描、跑策略、出报告、读取/复制策略、回测是同一种写法。

## 历史与复盘

```text
/compass-retro
```

或直接说：

```text
列出到期复盘；给 [市场名] 看完整历史时间线和相似市场教训。
```

- go 品：录实际日销、TACOS、退货率、净利率；
- no_go/waitlist：有新快照后做错杀回看；
- `validated`=原判断得到支持；`challenged`=建议人工复看，不自动翻案；`inconclusive`=证据不足；
- Lesson 必须附 `chk_*` / `dec_*` / `run_*` evidence；
- 调阈值前先做策略 backtest；对齐率要连「弃权」和「覆盖」一起看，覆盖不足一半时输出会标「样本不足」。

默认到期：go 30 天、testing 60 天、waitlist 45 天、no_go 90 天、review 30 天。自动历史速览可用 `/compass-history-brief on|off` 切换。

## 移动候选

```text
把 [市场名] 移动到 [深研/风控/决策/测品/复盘/归档]。
原因：[必须写指标、证据或审批结论]。
```

## 三种 Gate 结果

- `pass`：当前阶段通过；
- `review`：缺数据或边界项，先补证据；
- `reject`：触发硬门槛，通常归档。

最终经营决策必须另行记录：

```text
把 [市场名] 记录为 waitlist。
原因：[待补证据、复核条件和负责人]
```

可选状态：`go`、`waitlist`、`no_go`；每个状态都必须填写 reason，并保留在决策日志中。

## 三条红线

1. 不给 pi 账号密码或 API key；
2. 没有官方证据，不把风险判绿；
3. 不删除否决记录、不绕过预算熔断；不要直接读取或手改 `store.json`，历史查询用 `compass_history`。

完整说明见：[运营使用手册](./运营使用手册.md)。
