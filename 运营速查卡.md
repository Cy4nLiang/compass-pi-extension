# 罗盘运营速查卡

## 每次开始

```text
/compass
```

查看完整手册：

```text
/compass-help
```

如果命令不存在：

```text
/reload
```

## 每天先看待办

`/compass` 第 2 页「待办」：P1 当天处理、P2 别拖；事项解决后自动消失。会话里直接说：

```text
列出当前全部待办，先给 P1 和 P2。
```

（工具：`compass_todo`，可按优先级/类型/市场过滤。）

## Sorftime 调用与预算

预算页「调用」列显示各源当月 MCP 调用次数（自动计量）。要设单价或次数上限：

```text
配置 sorftime 预算：每次调用 0.5 元，每月上限 200 次。
```

达上限即熔断并拦截后续调用；解除靠提高上限（`monthly_call_limit`，0=清除）或等次月自动恢复。

## 导入 CSV

1. 把 CSV 放到项目的 `compass-imports/`；
2. 输入：

```text
/compass-import compass-imports/文件名.csv
```

3. 市场名称每次保持一致；
4. 选择正确来源：卖家精灵=`sellersprite`，Sorftime=`sorftime`；用户主动浏览器采集使用=`compass_browser`。

## 粗筛解释

```text
解释 [市场名] 的粗筛结果，列出 QRD、新品占比、CR3、AMZ占比，
逐条说明 Gate、来源、日期和缺失数据。
```

## 利润测算

```text
给 [市场名] 做利润测算并保存：售价__，采购__，头程__，关税__，
佣金__%，FBA__，CVR__%，CPC__，退货率__%，日销__，备货__天，
广告预算__，一次性费用__，总选品资金__。
```

金额统一使用同一种币种，建议美元。

## 风险核查

```text
核查 [市场名] 五类风险，只用最新官方来源并保留URL。
查不清填 unknown，需要专业判断填 review，不准凭记忆判绿。
```

## 完整策略与报告

```text
对 [市场名] 运行默认完整策略，解释全部非通过项。
```

```text
/compass-report [市场名]
```

报告默认写入 `.pi/compass/reports/`；自定义路径也必须在该目录内并使用 `.md`。自定义 `screen` 策略必须有 `market_screen` 规则，否则转人工复核。市场报告第 9 节会自动带上 OutcomeCheck 与命中的经验卡。

## 历史与复盘

```text
/compass-retro
```

或直接说：

```text
列出到期复盘；给 [市场名] 看完整历史时间线和相似市场教训。
```

- go 品：录实际日销、TACOS、退货率、净利率；
- no_go/waitlist：有新快照后做错杀回看；
- `validated`=原判断得到支持；`challenged`=建议人工复看，不自动翻案；`inconclusive`=证据不足；
- Lesson 必须附 `chk_*` / `dec_*` / `run_*` evidence；
- 调阈值前先做策略 backtest。

默认到期：go 30 天、testing 60 天、waitlist 45 天、no_go 90 天、review 30 天。自动历史速览可用 `/compass-history-brief on|off` 切换。

## 移动候选

```text
把 [市场名] 移动到 [深研/风控/决策/测品/复盘/归档]。
原因：[必须写指标、证据或审批结论]。
```

## 三种 Gate 结果

- `pass`：当前阶段通过；
- `review`：缺数据或边界项，先补证据；
- `reject`：触发硬门槛，通常归档。

最终经营决策必须另行记录：

```text
把 [市场名] 记录为 waitlist。
原因：[待补证据、复核条件和负责人]
```

可选状态：`go`、`waitlist`、`no_go`；每个状态都必须填写 reason，并保留在决策日志中。

## 三条红线

1. 不给 pi 账号密码或 API key；
2. 没有官方证据，不把风险判绿；
3. 不删除否决记录、不绕过预算熔断；不要直接读取或手改 `store.json`，历史查询用 `compass_history`。

完整说明见：[运营使用手册](./运营使用手册.md)。
